# @package _global_
defaults:
  - /data: ants
  - /dataset: ants/v6

subject: ants
version: v6
annotations: "no"    # yes | no | partial  (quoted: bare `no` parses as boolean false)

# Processing overwrite flags
overwrite:
  videos: false
  frames: false
  annotations: false
  hf: false
  embeddings: false
  tracking: false
  color_bounds: false
