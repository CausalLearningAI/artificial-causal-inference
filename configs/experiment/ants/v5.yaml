# @package _global_
defaults:
  - /data: ants
  - /dataset: ants/v5

subject: ants
version: v5
annotations: no    # yes | no | partial

# Processing overwrite flags
overwrite:
  videos: false
  frames: false
  annotations: false
  hf: false
  embeddings: false
  tracking: false
  color_bounds: false  

