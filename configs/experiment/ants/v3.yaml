# @package _global_
defaults:
  - /data: ants
  - /dataset: ants/v3

subject: ants
version: v3
annotations: yes   # yes | no | partial

# Processing overwrite flags
overwrite:
  videos: false
  frames: false
  annotations: false
  hf: false
  embeddings: false
  tracking: false
  color_bounds: false
