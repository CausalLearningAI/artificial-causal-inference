# @package _global_
defaults:
  - /data: mice
  - /dataset: mice/v1

subject: mice
version: v1

# Processing overwrite flags
overwrite:
  videos: false
  frames: false
  annotations: false
  hf: false
  embeddings: false

