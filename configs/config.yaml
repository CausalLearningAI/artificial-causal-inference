# @package _global_
defaults:
  - experiment: ants/v1
  - _self_

# Global paths (shared across all experiments)
paths:
  data_dir: ./data
  datasets_dir: ./dataset
  cache_dir: ./cache
  results_dir: ./results

