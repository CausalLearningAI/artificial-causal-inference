# Core dependencies
# Build pandas from source to ensure NumPy 2.4.1 ABI compatibility
# (pre-compiled wheels built against NumPy 1.x will crash with NumPy 2.4.1)
--no-binary pandas
--no-binary matplotlib
numpy==2.4.1
pandas>=2.3.0
scipy>=1.15.0
pyyaml>=6.0

# Configuration & Data Handling
hydra-core>=1.3.0
omegaconf>=2.3.0
huggingface-hub>=0.20.0

# Computer Vision & Video Processing
# OpenCV for reliable video encoding (replaces imageio for video generation)
opencv-python>=4.8.0
opencv-contrib-python>=4.8.0
pillow>=10.0.0
imageio>=2.14.0

# Deep Learning
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
datasets>=2.20.0

# Machine Learning
xgboost>=2.0.0
scikit-learn>=1.3.0

# Visualization & Notebooks
# matplotlib rebuilt from source to match NumPy 2.4.1
matplotlib>=3.10.0
seaborn>=0.12.0
jupyter>=1.0.0
ipykernel>=6.25.0
notebook>=7.0.0
ipywidgets>=8.0.0

# Utilities
tqdm>=4.65.0
loguru>=0.7.0

# Notes on environment setup:
# 1. NumPy/ABI: This environment uses NumPy 2.4.1. Packages (pandas, matplotlib, etc.) 
#    must be built from source (--no-binary) to avoid pre-compiled wheel incompatibilities.
# 2. Optional dependencies removed: numexpr, bottleneck (not needed, were causing rebuilds)
# 3. Video: Uses opencv-python for video encoding instead of imageio plugins
